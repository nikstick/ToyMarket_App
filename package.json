{
  "private": true,
  "workspaces": [
    "common",
    "bot",
    "backend"
  ],
  "scripts": {
    "build": "yarn tsc --build --force --verbose",
    "linking": "yarn workspaces foreach --all link",
    "prod": "python pack.py"
  },
  "version": "1.0.0",
  "packageManager": "yarn@4.4.1",
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^5.5.4"
  },
  "dependencies": {}
}
